{
  "name": "crop-steering-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Local stdio MCP tools for reviewed Crop Steering configuration in Home Assistant",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "~5.9.3"
  }
}
